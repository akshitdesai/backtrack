// globalTheme.js
import { makeStyles } from "@material-ui/core";

const globalStyles = makeStyles(() => ({
  root: {
    fontFamily: "Inter, Arial, sans-serif", // Change to the font family you want to use (e.g., 'Inter')
    // Other global styles
  },
}));

export default globalStyles;
